# v2.3

- NPC Rating 上限为 16465，旧存档中的超限数值在加载时修正。
- 电压更名为 elubos，保留好友关系、支线、私聊、已读状态和拼机伙伴。
- 旷工每班记入当月工资扣款（¥273），不立即扣钱包现金；每月 1 日发薪时结算上月扣款，累计扣款不超过 ¥6000。钱包显示当前月薪余额。旧版已经扣过的现金不重复计入扣薪。
- 曲库与选曲支持别名搜索，兼容难度、标准／DX 等现有筛选。别名来自落雪公开 API 的本地快照，覆盖 1077 首本地曲目；通过 `npm run refresh-aliases` 更新。
- 界面版本统一显示 `v大版本.小版本`，本次为 v2.3；内部仍使用构建标识检测更新和刷新缓存。
- 更新弹窗只显示本次版本的简短说明，保留保存进度并刷新的按钮。

别名来源：https://maimai.lxns.net/api/v0/maimai/alias/list
